use wasm_bindgen::prelude::*;
use regex::Regex;

#[wasm_bindgen]
pub fn add(a: i32, b: i32) -> i32 {
    a + b
}

fn correctQuestion(question: i32, input: &str) -> bool {
    match question {

    }
    true
}
